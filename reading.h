#include <string>
#include <vector>
#include <sstream>
#include <fstream>
#include <map>
using namespace std;

void read_potok(ifstream& file,string& str_word,map<string,int> &chis_slov,int*counts);